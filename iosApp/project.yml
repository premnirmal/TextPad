name: TextPad
options:
  bundleIdPrefix: com.github.premnirmal.textpad
  deploymentTarget:
    iOS: "17.0"
  createIntermediateGroups: true

configFiles:
  Debug: Configuration/Config.xcconfig
  Release: Configuration/Config.xcconfig

settings:
  base:
    DEVELOPMENT_TEAM: $(TEAM_ID)

targets:
  TextPad:
    type: application
    platform: iOS
    sources:
      - path: iosApp
    dependencies:
      - target: TextPadWidget
        embed: true
    settings:
      base:
        INFOPLIST_FILE: iosApp/Info.plist
        CODE_SIGN_ENTITLEMENTS: iosApp/iosApp.entitlements
        PRODUCT_NAME: $(APP_NAME)
        PRODUCT_BUNDLE_IDENTIFIER: $(BUNDLE_ID)
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        ASSETCATALOG_COMPILER_GLOBAL_ACCENT_COLOR_NAME: AccentColor
        DEVELOPMENT_ASSET_PATHS: '"iosApp/Preview Content"'
        ENABLE_PREVIEWS: YES
        SWIFT_VERSION: "5.0"
        TARGETED_DEVICE_FAMILY: "1,2"
        LD_RUNPATH_SEARCH_PATHS:
          - $(inherited)
          - "@executable_path/Frameworks"
    preBuildScripts:
      - name: Compile Kotlin Framework
        basedOnDependencyAnalysis: false
        script: |
          cd "$SRCROOT/.."
          ./gradlew :shared:embedAndSignAppleFrameworkForXcode

  TextPadWidget:
    type: app-extension
    platform: iOS
    sources:
      - path: TextPadWidget
    settings:
      base:
        INFOPLIST_FILE: TextPadWidget/Info.plist
        CODE_SIGN_ENTITLEMENTS: TextPadWidget/TextPadWidget.entitlements
        PRODUCT_NAME: TextPadWidget
        PRODUCT_BUNDLE_IDENTIFIER: $(BUNDLE_ID).widget
        ASSETCATALOG_COMPILER_GLOBAL_ACCENT_COLOR_NAME: AccentColor
        SWIFT_VERSION: "5.0"
        TARGETED_DEVICE_FAMILY: "1,2"
        GENERATE_INFOPLIST_FILE: NO
        SKIP_INSTALL: YES
    dependencies:
      - sdk: WidgetKit.framework
      - sdk: SwiftUI.framework

schemes:
  TextPad:
    build:
      targets:
        TextPad: all
        TextPadWidget: all
    run:
      config: Debug
    test:
      config: Debug
    archive:
      config: Release